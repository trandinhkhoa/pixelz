Based on `project_description.md`, create the following specs and save it in `docs/specs.md`:
- Create: Project overview — one paragraph, what it is and who it's for
- Create: Core features — what the app does, not how
- Create: User flow — step-by-step how a user moves through the app
- Create: Tech stack — languages, frameworks, databases, hosting
- Create: Out of scope — explicitly what you're not building (prevents AI from gold-plating)
- Create: Data models — key entities and their relationships
- Create: API contracts — endpoints if it's a backend, even rough ones
- Create: Constraints — performance requirements, platform targets, budget limits
- Create:Open questions — unresolved decisions so the AI knows to ask rather than assume

